<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>GalaxyTunes - Connexion</title>
    <!-- Inclure Font Awesome pour les icônes -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.5.1/css/all.min.css" integrity="sha512-DTOQO9RWCH3ppGqcWaEA1BIZOC6xxalwEsw9c2QQeAIftl+Vegovlnee1c9QX4TctnWMn13TZye+giMm8e2LwA==" crossorigin="anonymous" referrerpolicy="no-referrer" />
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: 'Arial', sans-serif;
        }

        body {
            height: 100vh;
            background: #0d1b2a;
            overflow: hidden;
            display: flex;
            justify-content: center;
            align-items: center;
            position: relative;
        }

        .galaxy-bg {
            position: absolute;
            width: 100%;
            height: 100%;
            background: radial-gradient(circle, rgba(233, 69, 96, 0.15) 0%, transparent 70%);
            animation: pulseNebula 15s ease-in-out infinite;
            z-index: 0;
        }

        .main-container {
            display: flex;
            justify-content: center;
            align-items: center;
            gap: 1rem; /* Espace réduit entre les sections */
            width: 100%;
            max-width: 1200px;
        }

        /* Section de connexion (gauche) */
        .login-container {
            position: relative;
            width: 450px;
            height: 600px;
            padding: 2rem; /* Padding réduit */
            background: linear-gradient(135deg, rgba(13, 27, 42, 0.95), rgba(27, 38, 59, 0.85));
            border-radius: 20px;
            box-shadow: 0 0 40px rgba(233, 69, 96, 0.2);
            z-index: 2;
        //    animation: floatContainer 5s ease-in-out infinite;
            border: 1px solid rgba(233, 69, 96, 0.4);
        }

        .logo {
            width: 80px;
            height: 80px;
            margin: 0 auto 1rem; /* Marge réduite */
            position: relative;
            perspective: 1000px;
        }

        .logo-inner {
            width: 100%;
            height: 100%;
            position: relative;
            transform-style: preserve-3d;
            animation: rotate3D 8s linear infinite;
        }

        .logo-inner::before {
            content: '♪';
            position: absolute;
            width: 100%;
            height: 100%;
            display: flex;
            align-items: center;
            justify-content: center;
            font-size: 2rem;
            color: #e94560;
            text-shadow: 0 0 15px rgba(233, 69, 96, 0.8);
        }

        .logo-inner::after {
            content: '';
            position: absolute;
            width: 80%;
            height: 80%;
            border: 3px solid #778da9;
            border-radius: 50%;
            border-style: dashed;
            animation: spinOrbit 4s linear infinite reverse;
        }

        .app-name {
            text-align: center;
            font-size: 1.8rem;
            font-weight: 900;
            letter-spacing: 3px;
            background: linear-gradient(90deg, #e94560, #778da9, #e94560);
            background-size: 200%;
            -webkit-background-clip: text;
            background-clip: text;
            -webkit-text-fill-color: transparent;
            animation: gradientFlow 6s ease infinite;
            margin-bottom: 1rem; /* Marge réduite */
        }

        .form-group {
            position: relative;
            margin-bottom: 1rem; /* Marge réduite */
        }

        .form-input {
            width: 100%;
            padding: 0.8rem 1.5rem 0.8rem 3rem; /* Padding ajusté */
            background: rgba(255, 255, 255, 0.03);
            border: none;
            border-radius: 50px;
            color: #fff;
            font-size: 1rem;
            box-shadow: inset 0 0 15px rgba(0, 0, 0, 0.4);
            transition: all 0.5s ease;
        }

        .form-input:focus {
            outline: none;
            box-shadow: 0 0 20px rgba(233, 69, 96, 0.6);
            background: rgba(255, 255, 255, 0.08);
        }

        .form-input::placeholder {
            color: rgba(255, 255, 255, 0.5);
            font-style: italic;
        }

        .form-icon {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
            color: rgba(233, 69, 96, 0.8);
            font-size: 1.2rem;
        }

        .login-btn {
            width: 100%;
            padding: 0.8rem; /* Padding ajusté */
            background: linear-gradient(45deg, #e94560, #415a77, #e94560);
            background-size: 200%;
            border: none;
            border-radius: 50px;
            color: #fff;
            font-size: 1rem; /* Taille de police ajustée */
            font-weight: bold;
            text-transform: uppercase;
            cursor: pointer;
            box-shadow: 0 0 20px rgba(233, 69, 96, 0.4);
            transition: all 0.5s ease;
            animation: gradientFlow 4s ease infinite;
        }

        .login-btn:hover {
            transform: scale(1.05) translateY(-5px);
            box-shadow: 0 0 40px rgba(233, 69, 96, 0.8);
        }

        .extra-options {
            text-align: center;
            margin-top: 1rem;
        }

        .forgot-password, .signup-link {
            display: block;
            color: #778da9;
            text-decoration: none;
            font-size: 0.9rem;
            margin: 0.5rem 0;
            transition: all 0.3s ease;
        }

        .forgot-password:hover, .signup-link:hover {
            color: #e94560;
            text-shadow: 0 0 10px rgba(233, 69, 96, 0.6);
        }

        .social-login {
            display: flex;
            flex-direction: column;
            gap: 0.5rem; /* Espace réduit entre les boutons */
            margin-top: 1rem;
        }

        .social-btn {
            width: 100%;
            padding: 0.8rem; /* Padding ajusté */
            border: none;
            border-radius: 25px;
            color: #fff;
            font-size: 0.9rem;
            cursor: pointer;
            transition: all 0.3s ease;
            display: flex;
            align-items: center;
            justify-content: center;
            gap: 0.5rem;
        }

        .social-btn.facebook {
            background: #3b5998;
        }

        .social-btn.gmail {
            background: #db4437;
        }

        .social-btn.icloud {
            background: #3693f3;
        }

        .social-btn:hover {
            transform: translateY(-3px);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.3);
        }

        /* Section carrousel (droite) */
        .carousel-container {
            position: relative;
            width: 450px;
            height: 600px;
            overflow: hidden;
            border-radius: 20px;
            box-shadow: 0 0 40px rgba(233, 69, 96, 0.2);
            z-index: 1;
        }

        .carousel {
            display: flex;
            width: 300%;
            height: 100%;
            animation: slide 12s infinite ease-in-out;
        }

        .carousel-item {
            width: 33.33%;
            height: 100%;
            background-size: cover;
            background-position: center;
            position: relative;
            transition: all 0.5s ease;
        }

        .carousel-item:nth-child(1) {
            background-image: url('https://img.freepik.com/photos-premium/belle-femme-ecoutant-musique-mur-jaune-charmante-dame-posant-yeux-fermes-aime-ecouter-sa-musique-preferee-garde-mains-ecouteurs-chante-se-detend-concept-mode-vie_176532-6745.jpg');
        }

        .carousel-item:nth-child(2) {
            background-image: url('https://img.freepik.com/photos-premium/femme-musique-vieille-femme-senior-dame-portrait-style-vie-mature-personnes-agees-personne-caucasienne-attrayant-son-casque-beaute-bonheur-adulte-heureux-blanc_163305-237379.jpg');
        }

        .carousel-item:nth-child(3) {
            background-image: url('https://img.freepik.com/photos-gratuite/jeune-garcon-ecouteurs_23-2148551127.jpg');
        }

        .carousel-item::before {
            content: '';
            position: align
            width: 100%;
            height: 100%;
            background: linear-gradient(to bottom, transparent, rgba(0, 0, 0, 0.7));
        }

        .carousel-caption {
            position: absolute;
            bottom: 20px;
            left: 20px;
            color: #fff;
            font-size: 1.2rem;
            text-shadow: 0 2px 4px rgba(0, 0, 0, 0.5);
            opacity: 0;
            animation: fadeInCaption 12s infinite ease-in-out;
        }

        /* Annonce */
        .announcement {
            position: absolute;
            bottom: 20px;
            width: 300px;
            padding: 0.8rem;
            background: rgba(233, 69, 96, 0.2);
            border-radius: 15px;
            color: #fff;
            font-size: 0.9rem;
            text-align: center;
            box-shadow: 0 0 20px rgba(233, 69, 96, 0.4);
            animation: pulseAnnouncement 3s ease infinite;
        }

        /* Étoiles et nébuleuses */
        .nebula {
            position: absolute;
            border-radius: 50%;
            background: rgba(233, 69, 96, 0.2);
            filter: blur(20px);
            animation: drift 20s ease-in-out infinite;
        }

        .nebula:nth-child(1) { width: 200px; height: 200px; top: 10%; left: 15%; }
        .nebula:nth-child(2) { width: 150px; height: 150px; top: 70%; left: 75%; animation-delay: 5s; }

        .star {
            position: absolute;
            width: 3px;
            height: 3px;
            background: #fff;
            border-radius: 50%;
            box-shadow: 0 0 5px rgba(255, 255, 255, 0.8);
            animation: twinkle 3s infinite;
        }

        .star:nth-child(3) { top: 25%; left: 20%; animation-delay: 0.5s; }
        .star:nth-child(4) { top: 65%; left: 85%; animation-delay: 1s; }
        .star:nth-child(5) { top: 45%; left: 35%; animation-delay: 2s; }

        /* Animations */
        @keyframes pulseNebula {
            0%, 100% { transform: scale(1); opacity: 0.8; }
            50% { transform: scale(1.05); opacity: 1; }
        }

        @keyframes rotate3D {
            0% { transform: rotateY(0deg) rotateX(0deg); }
            50% { transform: rotateY(180deg) rotateX(20deg); }
            100% { transform: rotateY(360deg) rotateX(0deg); }
        }

        @keyframes spinOrbit {
            from { transform: rotate(0deg); }
            to { transform: rotate(360deg); }
        }

        @keyframes floatContainer {
            0%, 100% { transform: translateY(0) rotate(0deg); }
            50% { transform: translateY(-15px) rotate(1deg); }
        }

        @keyframes gradientFlow {
            0% { background-position: 0% 50%; }
            50% { background-position: 100% 50%; }
            100% { background-position: 0% 50%; }
        }

        @keyframes twinkle {
            0%, 100% { opacity: 0.3; transform: scale(1); }
            50% { opacity: 1; transform: scale(1.2); }
        }

        @keyframes drift {
            0%, 100% { transform: translate(0, 0); }
            25% { transform: translate(20px, -20px); }
            75% { transform: translate(-20px, 20px); }
        }

        @keyframes slide {
            0%, 25% { transform: translateX(0); }
            33.33%, 58.33% { transform: translateX(-33.33%); }
            66.66%, 91.66% { transform: translateX(-66.66%); }
            100% { transform: translateX(0); }
        }

        @keyframes fadeInCaption {
            0%, 25%, 66.66%, 100% { opacity: 0; }
            8.33%, 58.33% { opacity: 1; }
        }

        @keyframes pulseAnnouncement {
            0%, 100% { transform: scale(1); opacity: 0.8; }
            50% { transform: scale(1.05); opacity: 1; }
        }
    </style>
</head>
<body>
    <div class="galaxy-bg"></div>

    <!-- Conteneur principal avec les deux sections côte à côte -->
    <div class="main-container">
        <!-- Section de connexion (gauche) -->
        <div class="login-container">
            <div class="logo">
                <div class="logo-inner"></div>
            </div>
            <h1 class="app-name">GalaxyTunes</h1>

            <form class="login-form">
                <div class="form-group">
                    <i class="fas fa-user form-icon"></i>
                    <input 
                        type="text" 
                        class="form-input" 
                        placeholder="Votre pseudo" 
                        required
                    >
                </div>

                <div class="form-group">
                    <i class="fas fa-lock form-icon"></i>
                    <input 
                        type="password" 
                        class="form-input" 
                        placeholder="Votre mot de passe" 
                        required
                    >
                </div>

                <button type="submit" class="login-btn">Lancer l'odyssée</button>
            </form>

            <!-- Options supplémentaires -->
            <div class="extra-options">
                <a href="#" class="forgot-password">Mot de passe oublié ?</a>
                <a href="create_account" class="signup-link">Créer un compte</a>
                <div class="social-login">
                    <button class="social-btn facebook">
                        <i class="fab fa-facebook-f"></i> Se connecter avec Facebook
                    </button>
                    <button class="social-btn gmail">
                        <i class="fab fa-google"></i> Se connecter avec Gmail
                    </button>
                    <button class="social-btn icloud">
                        <i class="fab fa-apple"></i> Se connecter avec iCloud
                    </button>
                </div>
            </div>
        </div>

        <!-- Section carrousel (droite) -->
        <div class="carousel-container">
            <div class="carousel">
                <div class="carousel-item">
                    <div class="carousel-caption">Musique classique</div>
                </div>
                <div class="carousel-item">
                    <div class="carousel-caption">House</div>
                </div>
                <div class="carousel-item">
                    <div class="carousel-caption">Motivation</div>
                </div>
            </div>
        </div>
    </div>

    <!-- Annonce -->
    <div class="announcement">
        Écoutez vos hits préférés en illimité dès maintenant !
    </div>

    <!-- Décorations cosmiques -->
    <div class="nebula"></div>
    <div class="nebula"></div>
    <div class="star"></div>
    <div class="star"></div>
    <div class="star"></div>
</body>
</html>